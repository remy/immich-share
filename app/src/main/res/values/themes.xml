<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose supplies the real Material 3 theme (dynamic colour, dark mode).
      These XML themes only cover the window before Compose draws: a normal
      opaque window for MainActivity, and a transparent one for ShareActivity so
      the app the user shared from stays visible behind the confirm sheet.
    -->
    <style name="Theme.ImmichShare" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.ImmichShare.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
